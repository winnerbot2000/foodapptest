/// Filenames used by `JSONFileStore` for persisting model collections.
struct StorageKeys {
    static let restaurants = "restaurants.json"
    static let dishes = "dishes.json"
    static let entries = "entries.json"
    static let menus = "menus.json"
}